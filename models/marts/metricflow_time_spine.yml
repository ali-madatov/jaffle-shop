models:
  - name: metricflow_time_spine
    description: A time spine table with one row per day, used by MetricFlow for time-based joins and aggregations.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
